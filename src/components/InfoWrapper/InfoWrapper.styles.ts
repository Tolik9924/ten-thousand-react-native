import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	wrapper: {
		backgroundColor: '#fff',
		padding: 12,
		boxSizing: 'border-box',
		borderRadius: 16,
	},
	border: {
		borderWidth: 1,
		borderColor: '#CED5E0',
	},
});
